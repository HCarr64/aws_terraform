# aws_terraform

This is to get better at terraform and learn AWS a bit more in a non maintenance way.

I've created a basic VPC manually in AWS with an EC2 instance with nginx installed that I can access via public browser.

The goal of this repo is to recreate that.
